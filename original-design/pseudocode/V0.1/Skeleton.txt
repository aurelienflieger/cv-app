class Skeleton {

render() {
    const {children, currentSection, isRegularScreen} = this.props;
    return (
        <Header currentSection={currentSection}/>
        <Main>
        <Hook currentSection={currentSection}/>
        <CounterAndSectionName currentSection={currentSection}/>
        {children}
        {isRegularScreen && <SidePicture currentSection={currentSection}/>}
        </Main>
    )
 }
}