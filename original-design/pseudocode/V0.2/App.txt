const App = () => {
  const [isFirstLanding, toggleHomeScreen] = useState(true);
  return (
    <div className="App">
      {isFirstLanding && (
        <HomeScreen toggleHomeScreen={toggleHomeScreen} />
      ) : (
        <FormManager />
      )}
    </div>
  );
};